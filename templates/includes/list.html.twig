<div class="container photographers-list">
    <h2 class="title is-2 has-text-dark has-text-centered mb-6">Choose a photographer in your location</h2>
    {% for photo in photographers %}
        {% if loop.index0 % 3 == 0 %}
            <div class="is-flex is-justify-content-space-around">
        {% endif %}
                {% include 'includes/card.html.twig' %}

        {% if (loop.index0 + 1) % 3 == 0 or loop.last %}
            </div>
        {% endif %}
    {% endfor %}
</div>
